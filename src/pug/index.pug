extends /_layout/_default-layout

append config-block
  - pageId = 'index'

block content-block
  mixin card-fv(opts = {})
    - const { title, actionArr } = opts;
    .card-fv
      .card.mdl-card.mdl-shadow--2dp
        .title.mdl-card__title.mdl-card--expand(style="background-image: url(img/fv-bg.jpg)")
          h2.title-text.mdl-card__title-text
            = title
        .supporting-text.mdl-card__supporting-text
          block
        if actionArr
          .actions.mdl-card__actions.mdl-card--border
            each action in actionArr
              - const { className, label, icon } = action;
              div(class=className)
                button.button.mdl-button.mdl-button--colored.mdl-js-button.mdl-js-ripple-effect
                  if label
                    = label
                  if icon
                    i.material-icons= icon

  +card-fv({
    title: 'コトダマFM',
    actionArr: [
      {
        className: 'btn-start',
        label: 'ラジオを収録する (PC限定)',
      },
      {
        className: 'btn-contact',
        icon: 'contact_mail',
      }
    ],
  })
    | きみの大切な人に声を届けます。
    br
    | きみのトークと好きな曲を収録して、番組を制作しよう！
